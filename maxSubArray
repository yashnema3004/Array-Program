class Solution:
    def maxSubArray(self, nums: List[int]) -> int:
        total = nums[0]
        maxTotal = nums[0]

        for i in range(1,len(nums)-1):
            total = max(total , nums[i] + total)
            maxTotal = max(maxTotal , total)
    
        return maxTotal
            









